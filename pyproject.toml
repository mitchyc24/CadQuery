[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cqlib"
version = "0.1.0"
description = "Shared CadQuery helpers used by every model in this repo"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["cqlib"]
